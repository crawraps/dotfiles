# utilities

essential CLI tools and utilities.

**Preference key:** `preferences.modules.utilities`
**Default:** `true`

**Files:**
- `home.nix` — fd, fastfetch, jq, playerctl, translate-shell, file, tree, wget, ripgrep, killall, libnotify, unzip, zip, ntfs3g, wl-clipboard, calc (from flake input)

**Dependencies:** none