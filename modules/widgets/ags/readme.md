# widgets / ags

ags (aylurs gtk shell) configuration.

**Preference key:** `preferences.modules.widgets.ags`
**Default:** `false`

**Files:**
- `home.nix` — copies ags config to `~/.config/ags/`
- `assets/config/` — ags config files

**Dependencies:** none