# keyboard

keyd keyboard remapping daemon.

**Preference key:** `preferences.modules.keyboard`
**Default:** `true`

**Files:**
- `system.nix` — enables keyd: caps lock → left alt, left alt → nav layer (hjkl as arrow keys)

**Dependencies:** none