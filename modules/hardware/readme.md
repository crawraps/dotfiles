# hardware

auto-generated hardware scan from `nixos-generate-config`.

**Preference key:** none (always imported via `configuration.nix`)

**Files:**
- `default.nix` — filesystem mounts, kernel modules, cpu microcode, DHCP default

**Dependencies:** none