# development

development toolchain packages.

**Preference key:** `preferences.modules.development`
**Default:** `true`

**Files:**
- `home.nix` — installs nodejs_22, cmake, meson, tree-sitter

**Dependencies:** none