# browser / chromium

chromium web browser.

**Preference key:** `preferences.modules.browser.chromium`
**Default:** `true`

**Files:**
- `home.nix` — installs chromium, creates desktop entry with new-window and incognito actions
- `assets/icons/` — custom svg icons for desktop entry

**Dependencies:** none