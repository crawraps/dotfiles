# screenshots

wayland screenshot and color picker tools.

**Preference key:** `preferences.modules.screenshots`
**Default:** `true`

**Files:**
- `home.nix` — grim, slurp, hyprpicker, hyprquickshot (from flake input)

**Dependencies:** compositor (requires wayland), `hyprquickshot` flake input