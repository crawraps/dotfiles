# ld

nix-ld dynamic linker compatibility layer.

**Preference key:** `preferences.modules.ld`
**Default:** `true`

**Files:**
- `system.nix` — enables nix-ld with common libraries (stdenv, zlib, fuse3, icu, nss, openssl, curl, expat)

**Dependencies:** none