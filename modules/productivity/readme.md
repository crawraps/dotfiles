# productivity

obsidian note-taking and workflow scripts.

**Preference key:** `preferences.modules.productivity`
**Default:** `true`

**Files:**
- `home.nix` — obsidian, mongodb-compass, `workflow` and `work-timer` scripts
- `scripts/` — `workflow`, `work-timer` (installed to `~/.local/bin/`)

**Dependencies:** none